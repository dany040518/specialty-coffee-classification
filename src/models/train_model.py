"""Entrenamiento de modelos (fase 04 - modelado).

TODO: el notebook original importa `LogisticRegression`, `DecisionTreeClassifier`,
`RandomForestClassifier`, `XGBClassifier`, `SMOTE`, `RandomUnderSampler`,
`SMOTEENN` y `StratifiedKFold`, pero ninguno llega a instanciarse ni a
entrenarse (no hay una sola llamada a `.fit()` en el notebook original).
Este módulo está vacío a propósito: no se entrena aquí ningún modelo ni se
fijan hiperparámetros que el autor no haya decidido.

Cuando se implemente 04_modelado.ipynb, el código de entrenamiento
(pipelines de imblearn, validación cruzada, comparación de modelos) debe
vivir aquí, leyendo sus hiperparámetros de config/config.yaml.
"""
